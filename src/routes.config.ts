/**
 * An enumeration of predefined application paths for routing purposes.
 *
 * This enum provides a clear and type-safe way to reference paths
 * within your application, promoting code maintainability and reducing the likelihood
 * of errors due to typos or incorrect path construction.
 *
 */
export enum Pathname {
  // Public
  HOME = "/",
}
